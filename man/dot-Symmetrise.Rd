% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{.Symmetrise}
\alias{.Symmetrise}
\title{Reconcile the two triangles of a distance matrix}
\usage{
.Symmetrise(d, tolerance = .SymmetryTolerance())
}
\arguments{
\item{d}{A square numeric matrix, known finite.}

\item{tolerance}{Numeric: largest scaled discrepancy to repair.}
}
\value{
\code{.Symmetrise()} returns an exactly symmetric matrix.
}
\description{
Internal helper: returns \code{d} unchanged when its triangles already agree,
averages them when they differ by no more than \code{tolerance}, and errors
beyond it.
}
\details{
The solvers read whichever of \code{d[i, j]} and \code{d[j, i]} is the cheaper memory
access, and which one that is depends on the subset size, so a matrix
symmetric only to rounding would answer differently for different \code{k}.
Averaging the two triangles settles the question once, at a cost of one
\code{n * n} copy: repair \code{d} yourself if calling a solver in a loop.
}
\keyword{internal}
