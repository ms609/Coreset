% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gonzalez.R
\name{.MaximinFromColumn}
\alias{.MaximinFromColumn}
\title{Gonzalez maximin from a distance-column oracle (worker)}
\usage{
.MaximinFromColumn(colFn, N, n, first, progress = FALSE)
}
\arguments{
\item{colFn}{Column oracle; see \code{\link[=Gonzalez]{Gonzalez()}}.}

\item{N}{Integer element count.}

\item{n}{Integer subset size (\verb{>= 2}).}

\item{first}{Integer seed index.}
}
\value{
Integer vector of selected indices.
}
\description{
Mirrors \code{MaximinFrom_cpp()}, substituting an on-demand \code{colFn(i)} call for
the matrix-column read \code{d[, i]}. \code{which.max()} uses R's
first-maximum (strict \code{>}) rule, matching the kernel's tie-breaking, so the
selection is identical to the matrix path on symmetric input.
}
\keyword{internal}
