% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{.AsMaxMinSelection}
\alias{.AsMaxMinSelection}
\title{Stamp the \code{MaxMinSelection} class onto a solver's index vector}
\usage{
.AsMaxMinSelection(x, producer)
}
\arguments{
\item{x}{Integer index vector carrying the solver's score attributes.}

\item{producer}{Character tag naming the solver (\code{"FarFirst"}, \code{"DropAdd"},
\code{"Grasp"}).}
}
\value{
\code{.AsMaxMinSelection()} returns \code{x} with \code{producer} attribute and
\code{"MaxMinSelection"} class, or \code{x} unchanged if it is empty.
}
\description{
The selection-returning solvers each already attach their score (and any
secondary attributes) via \code{\link[base:structure]{base::structure()}}; this adds the shared S3 class
and a \code{producer} tag the print method reads to name the algorithm. An empty
selection (\verb{length 0}) is left bare: there is nothing to describe.
}
\keyword{internal}
