% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate-utils.R
\name{PointDiameter}
\alias{PointDiameter}
\title{Diameter pair from coordinates (matrix-free)}
\usage{
PointDiameter(points)
}
\arguments{
\item{points}{A numeric \verb{N x dim} coordinate matrix (complete, no \code{NA}).}
}
\value{
Numeric vector \code{c(d_max, i, j)}: the maximum distance and the 1-based
indices of a pair achieving it.
}
\description{
Finds the maximum pairwise Euclidean distance and one realising pair,
computed from \code{points} in \code{O(N^2 * dim)} time and \code{O(N)} memory. The row
index matches the first below-diagonal maximum under column-major
\code{which.max} on the distance matrix, so it agrees with the matrix path.
}
\examples{
pts <- matrix(c(0, 0, 3, 4, 1, 1), ncol = 2, byrow = TRUE)
PointDiameter(pts)
}
\seealso{
\code{\link[=PointColumn]{PointColumn()}}, \code{\link[=Gonzalez]{Gonzalez()}}.
}
