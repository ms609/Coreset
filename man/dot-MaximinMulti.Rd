% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{.MaximinMulti}
\alias{.MaximinMulti}
\title{Gonzalez maximin from several starting indices at once}
\usage{
.MaximinMulti(k, firsts, d = NULL, points = NULL)
}
\arguments{
\item{k}{Integer: target subsample size (\verb{>= 1}).}

\item{firsts}{Integer vector of distinct first-selected indices, one per pass.}

\item{d}{Square pairwise distance matrix, or \code{NULL} when \code{points} is given.}

\item{points}{A \code{double} \verb{N x dim} coordinate matrix, or \code{NULL}.}
}
\value{
\code{.MaximinMulti()} returns a list with one \code{list(idx, tK)} per element
of \code{firsts}, in that order.
}
\description{
Ensemble counterpart of \code{\link[=.MaximinFrom]{.MaximinFrom()}} / \code{\link[=.MaximinFromPoints]{.MaximinFromPoints()}}: solves
one greedy pass per seed. Each pass is an independent function of its seed,
so under \code{mc.cores > 1} the passes run concurrently, one per thread; a lone
seed, or a problem large enough for a single pass to occupy every thread
itself, falls back to the per-pass parallelism instead.
}
\keyword{internal}
