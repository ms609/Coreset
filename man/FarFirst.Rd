% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{FarFirst}
\alias{FarFirst}
\title{Deterministic Gonzalez furthest-point selection}
\usage{
FarFirst(
  d = NULL,
  m,
  method = .kDefaultEnsemble,
  pivots = NULL,
  points = NULL,
  N = NULL,
  progress = getOption("MaxMin.progress", interactive())
)
}
\arguments{
\item{d}{A \code{dist} object, a square numeric matrix of pairwise distances, or
a distance function (see \emph{§Distance function}). Asymmetric matrices are
accepted; symmetry is not checked (an \code{O(N^2)} check is intentionally
omitted), and the algorithm treats \eqn{d_{ij}} and \eqn{d_{ji}} as
independent. Ignored when \code{points} is supplied.}

\item{m}{Integer: number of points to select. If \code{m > N}, all \code{N} indices
are returned in Gonzalez (farthest-first) order.}

\item{method}{Integer or character (scalar or vector); how to seed the
greedy pass (matching the \code{method} argument of \code{\link[=MaxMinSeed]{MaxMinSeed()}}). An
\strong{integer} gives the explicit 1-based index of the first selected point
(a single bare Gonzalez pass). A \strong{length-1 character} names a single
deterministic seeding strategy run as one bare pass: \code{"centroid"}
(coordinates only), \code{"peripheral"} (two-sweep diameter-endpoint
approximation), \code{"diameter"}, \code{"anti_medoid"}, \code{"medoid"}, \code{"rowsum"},
\code{"rownorm"}, or \code{"first"} (index 1). A \strong{length > 1 character vector} --
or the lone \code{"random_furthest"} token -- requests an ensemble: each named
anchor runs a full Gonzalez pass and the best result by \code{\link[=MinDist]{MinDist()}} is
returned with \code{strategy_results} and \code{winning_strategy} (character vector
of all tied-best strategies) attributes. The \code{"random_furthest"} token
expands to one start per element of \code{pivots}, labelled \code{random_furthest1},
\code{random_furthest2}, ...; named on its own it still runs the ensemble (one
pass per pivot), so a single random start is best obtained via
\code{\link[=MaxMinSeed]{MaxMinSeed()}}. Valid ensemble anchors: any subset of \code{c("centroid", "peripheral", "random_furthest", "diameter", "anti_medoid", "medoid", "rowsum", "rownorm")} (\code{"centroid"} requires \code{points}). Default:
\code{"random_furthest"} (three random starts; see \code{pivots}). See \code{\link[=MaxMinSeed]{MaxMinSeed()}}
for anchor definitions. On the distance-column oracle path only an integer
\code{method} is honoured; a named or ensemble \code{method} there warns and falls
back to the peripheral seed (see \emph{Distance-column oracle}).}

\item{pivots}{Integer vector of pivot indices over which the
\code{"random_furthest"} ensemble token expands: each pivot contributes one
start, seeded at the point furthest from it, so the vector's length sets
the number of random-furthest starts. Left unspecified, three pivots are
drawn with the session RNG (\code{sample.int(N, 3)}; set a seed for a
reproducible selection). Pass \code{integer(0)}, \code{NA}, or \code{NULL} to disable the
random starts, or an index vector to choose the pivots (and their count)
explicitly. Disabling the random starts errors under the default \code{method}
(which names only \code{"random_furthest"}, leaving no anchor); pair it with a
deterministic \code{method} such as \code{"peripheral"}.}

\item{points}{Optional \verb{N x dim} numeric coordinate matrix. When supplied,
the selection is computed directly from coordinates in \code{O(N * m * dim)}
time and \code{O(N)} memory, never materialising the \verb{N x N} distance matrix
(\code{d} is then unused). For Euclidean data the returned indices are
identical to the matrix path. Only complete (non-\code{NA}) data is supported.}

\item{N}{Integer: the total number of elements. Required (and used) only on
the distance-column oracle path, where it cannot be inferred from the
closure; ignored for the matrix and coordinate paths.}

\item{progress}{Logical; show a progress bar during greedy selection on the
distance-column oracle path (the only path slow enough to warrant one).
Default: \code{TRUE} in interactive sessions, \code{FALSE} otherwise
(\code{getOption("MaxMin.progress", interactive())}).}
}
\value{
Integer vector of length \code{min(m, N)} of selected indices, in
farthest-first (greedy) selection order -- \strong{not} sorted (unlike
\code{\link[=DropAdd]{DropAdd()}}, \code{\link[=Grasp]{Grasp()}} and \code{ExactMaxMin()$indices}, which return ascending
indices). The achieved \eqn{T_k} (the selection's minimum pairwise
distance) is attached as attribute \code{score}. An ensemble \code{method}
additionally carries \code{strategy_results} and \code{winning_strategy} attributes.
}
\description{
Greedy k-centre selection \insertCite{Gonzalez1985}{MaxMin}.
Iteratively selects the point furthest from the current selection, a
2-approximation to the k-centre problem.
The quality of the result depends on the first (seed) point; by default
\code{FarFirst()} runs three starts from randomly selected peripheral seeds.
The deterministic \code{O(N)} anchors (\code{"centroid"}, \code{"peripheral"}) and the
costlier \code{O(N^2)} anchors (\code{"diameter"}, \code{"anti_medoid"}, \code{"medoid"},
\code{"rowsum"}, \code{"rownorm"}) are alternative \code{seed} strategies.
}
\details{
Distances may be provided as:
\describe{
\item{a \strong{distance matrix} (\code{d})}{a \code{dist} object or square matrix, held
in full;}
\item{a \strong{coordinate matrix} (\code{points})}{each needed distance is
recomputed from coordinates on the fly in \code{O(N)} memory
(Euclidean data only);}
\item{a distance function (function passed as \code{d})}{for metrics
with neither a stored matrix nor a coordinate embedding, where distances
may be computed on demand.}
}
}
\section{Distance function}{

When \code{d} is a function, it will be passed a single 1-based index \code{i}, and
should return the distances from element \code{i} to every element in turn,
optionally omitting entry \code{i}, the self-distance.
The function will be called once per selected element, to avoid building a
complete \verb{N x N} matrix.
}

\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d <- dist(pts)
# Default: best of three random-furthest starts (set.seed for reproducibility):
FarFirst(d, 5L)
# More random-furthest starts (length of `pivots` sets the count):
FarFirst(d, 5L, pivots = sample.int(nrow(as.matrix(d)), 8L))
# Or choose the pivots explicitly:
FarFirst(d, 5L, pivots = c(1L, 10L, 20L))
# Custom two-anchor ensemble:
FarFirst(d, 5L, method = c("diameter", "anti_medoid"))
# A single strategy:
FarFirst(d, 5L, method = "diameter")
# An explicit start index (integer method):
FarFirst(d, 5L, method = 1L)
# Matrix-free coordinate path (identical result, O(N) memory):
FarFirst(m = 5L, points = pts, method = 1L)

# Distance-column oracle: supply one column at a time, never the full matrix.
data("USArrests")
arrestTypes <- USArrests[, c("Murder", "Assault", "Rape")]
StateDist <- function(i) {
  diffs <- sweep(arrestTypes, 2, unlist(arrestTypes[i, ]), "-")
  sqrt(rowSums(diffs ^ 2))
}
idx <- FarFirst(StateDist, m = 4L, N = nrow(arrestTypes), method = 1L)
arrestTypes[idx, ]
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=MaxMinSeed]{MaxMinSeed()}} for the seed indices alone; \code{\link[=DropAdd]{DropAdd()}} and
\code{\link[=ExactMaxMin]{ExactMaxMin()}} for higher-effort solvers.
}
