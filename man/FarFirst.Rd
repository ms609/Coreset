% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{FarFirst}
\alias{FarFirst}
\title{Greedy farthest-first point selection}
\usage{
FarFirst(
  k,
  d = NULL,
  points = NULL,
  N = NULL,
  strategy = "random_furthest",
  nSeeds = 3L
)
}
\arguments{
\item{k}{Integer: number of points to select.}

\item{d}{A \code{dist} object, a square numeric matrix of pairwise distances, or
a distance function that takes an index \code{i} and returns the distance from
\code{i} to each other element (optionally including the self-distance).
Ignored when \code{points} is supplied.}

\item{points}{Optional \verb{N x dim} numeric coordinate matrix. When supplied,
the selection is computed directly from coordinates in
\eqn{O(N \cdot k \cdot dim)} time and \eqn{O(N)} memory, which avoids
creating an \eqn{O(N^2)} distance matrix. Missing entries (\code{NA}) are not
supported.}

\item{N}{Integer: the total number of elements. Required (and used) only on
the distance-column oracle path, where it cannot be inferred from the
closure; ignored for the matrix and coordinate paths.}

\item{strategy}{Integer or character defining how to seed the greedy pass.
Pass the name of one or more seeding strategies described in \code{\link[=PickPoint]{PickPoint()}}
to run each strategy and return the best solution.}

\item{nSeeds}{Integer: number of distinct seeds to draw under the (default)
\code{"random_furthest"} strategy. Three starts captures most of the gain from
restarting — the improvement curve bends early (knee at n ≈ 3–4 across
benchmarks) and additional restarts add little. For higher-quality
solutions, prefer \code{\link[=DropAdd]{DropAdd()}}: tabu search escapes the farthest-first
construction family where restarts plateau.}
}
\value{
\code{FarFirst()} returns an integer vector with class \code{MaxMinSelection},
listing the selected indices in the order they were selected.
Attributes report:
\itemize{
\item \code{score}: the selection's minimum pairwise distance (\eqn{T_k}).
\item \code{winning_strategy}: character vector listing strategies that attained the
optimal score.
\item \code{strategy_results}: results for each strategy.
}
}
\description{
Greedy farthest-first selection \insertCite{Gonzalez1985,Hochbaum1985}{Coreset}
iteratively selects the point furthest from the current selection to yield a
2-approximation to the \emph{k}-centre and Max Min Diversity problems.
}
\section{Progress bar}{

In interactive sessions, the distance-column path shows a progress bar.
To toggle, set \code{options("Coreset.progress" = FALSE)} (or \code{TRUE}).
}

\section{Parallelism}{

To parallelize computation when OpenMP is available, set the \code{"mc.cores"}
option:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(mc.cores = 2L)                       # use a fixed number of cores
options(mc.cores = parallel::detectCores())  # or all available cores
}\if{html}{\out{</div>}}

The main use case for parallelization is when \code{nSeeds} is a multiple of
\code{mc.cores}, so each seed point can be evaluated in parallel.
}

\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d <- dist(pts)

# Default: best of three random-furthest starts (set.seed for reproducibility):
FarFirst(5L, d)

# More random-furthest starts:
FarFirst(5L, d, nSeeds = 15L)

# Custom two-anchor ensemble:
FarFirst(5L, d, strategy = c("diameter", "anti_medoid"))

# A single strategy:
FarFirst(5L, d, strategy = "diameter")

# An explicit start index (integer strategy):
FarFirst(5L, d, strategy = 1L)

# Matrix-free coordinate path (identical result, O(N) memory):
FarFirst(5L, points = pts, strategy = 1L)

# Distance-column oracle: supply one column at a time, never the full matrix.
data("USArrests")
arrestTypes <- USArrests[, c("Murder", "Assault", "Rape")]
StateDist <- function(i) {
  diffs <- sweep(arrestTypes, 2, unlist(arrestTypes[i, ]), "-")
  sqrt(rowSums(diffs ^ 2))
}
idx <- FarFirst(4L, StateDist, N = nrow(arrestTypes), strategy = 1L)
arrestTypes[idx, ]
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=PickPoint]{PickPoint()}} for the seed indices alone; \code{\link[=DropAdd]{DropAdd()}} and
\code{\link[=ExactMaxMin]{ExactMaxMin()}} for higher-effort solvers.
}
