% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exact.R
\name{ExactMaxMin}
\alias{ExactMaxMin}
\title{Exact Max-Min Diversity Problem solution}
\usage{
ExactMaxMin(
  k,
  d,
  maxSeconds = 60,
  warmStart = NULL,
  nStart = 8L,
  graspPlateau = 50L,
  dropPlateau = 512L
)
}
\arguments{
\item{k}{Integer: target subset size, between 2 and \code{nrow(d)}.}

\item{d}{\code{dist} object or a square symmetric numeric distance matrix.}

\item{maxSeconds}{Numeric: search terminates after this many seconds have
elapsed, returning largest threshold proven feasible.}

\item{warmStart}{Optional integer vector giving indices of a candidate subset to add
to the heuristic warm-start pool.}

\item{nStart}{Integer: how many \code{\link[=Grasp]{Grasp()}} restarts enter the warm-start
pool.}

\item{graspPlateau, dropPlateau}{Integer: the stopping plateaus given to the
pool's \code{\link[=Grasp]{Grasp()}} restarts and its \code{\link[=DropAdd]{DropAdd()}} pass. Deeper searches cost
more, but raise the lower bound the exact search starts from.}
}
\value{
\code{ExactMaxMin()} returns an integer vector of length \code{k} (sorted
ascending) with class \code{"MaxMinSelection"}, carrying attributes:
\describe{
\item{score}{The minimum pairwise distance within the selection.
When \code{proven} is \code{TRUE} this is the optimum; otherwise
a lower bound.}
\item{proven}{Logical: \code{TRUE} if the search certified optimality within
the budget, \code{FALSE} if it returned an unproven incumbent.}
\item{time_s}{Wall-clock seconds elapsed.}
\item{N, k}{Instance size and target subset size.}
}
Prints as a terse summary via \code{\link[=print.MaxMinSelection]{print.MaxMinSelection()}}.
}
\description{
\code{ExactMaxMin()} finds the optimal solution to the Max-Min Diversity Problem
(discrete \emph{p}-dispersion) by iterated node-packing
\insertCite{Sayyady2016}{Coreset} (which may be slow or intractable on large
sets).
}
\details{
The search is warm-started from a heuristic lower bound (the best of several
\code{\link[=Grasp]{Grasp()}} restarts and a \code{\link[=DropAdd]{DropAdd()}} pass), then gallops upward from that
bound to the first infeasible threshold and bisects the resulting bracket.

To parallelize computation when OpenMP is available, set the \code{"mc.cores"}
option:
\preformatted{
options(mc.cores = 2L)                       # use a fixed number of cores
options(mc.cores = parallel::detectCores())  # or all available cores
}
Parallelization returns identical results under a given seed.
}
\section{Progress bar}{

In interactive sessions, a progress indicator is shown.
To toggle, set \code{options("Coreset.progress" = FALSE)} (or \code{TRUE}).
}

\examples{
set.seed(1)
pts <- matrix(rnorm(18), ncol = 2)
ExactMaxMin(3L, dist(pts))
}
\references{
\insertAllCited{}
}
