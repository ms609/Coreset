% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate-utils.R
\name{PointColumn}
\alias{PointColumn}
\title{Euclidean distance column from coordinates (matrix-free)}
\usage{
PointColumn(points, i)
}
\arguments{
\item{points}{A numeric \verb{N x dim} coordinate matrix (complete, no \code{NA}).}

\item{i}{Integer 1-based point index.}
}
\value{
Numeric vector of length \code{N}; the self-distance at position \code{i} is 0.
}
\description{
Returns the distances from point \code{i} to every point, computing them on the
fly from \code{points} (\code{O(N * dim)} time, \code{O(N)} memory) and reproducing
\code{stats::dist()}'s Euclidean bits exactly. This is column \code{i} of the
distance matrix, never materialising the matrix.
}
\examples{
pts <- matrix(c(0, 0, 3, 4), ncol = 2, byrow = TRUE)
PointColumn(pts, 1L)  # c(0, 5)
}
\seealso{
\code{\link[=PointDiameter]{PointDiameter()}}, \code{\link[=Gonzalez]{Gonzalez()}}.
}
