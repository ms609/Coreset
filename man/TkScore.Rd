% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{MinDist}
\alias{MinDist}
\title{Minimum pairwise distance within a selection (T_k = k-centre objective)}
\usage{
MinDist(d = NULL, idx, points = NULL)
}
\arguments{
\item{d}{Pairwise distance matrix or \code{dist} object. Ignored when \code{points}
is supplied.}

\item{idx}{Integer vector of selected row/col indices.}

\item{points}{Optional \verb{N x dim} numeric coordinate matrix. When supplied,
the score is computed from \code{stats::dist()} on the selected
sub-coordinates only (\verb{k x k}), never the full \verb{N x N} matrix (\code{d} is then
unused). For Euclidean data the result is identical to the matrix path.}
}
\value{
Numeric scalar; \code{NA_real_} if \code{length(idx) < 2}.
}
\description{
Returns the minimum pairwise distance among selected points, the canonical
k-centre objective \eqn{T_k}. Higher values indicate a more spread-out
(better) selection.
}
\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d <- dist(pts)
MinDist(d, Gonzalez(d, 5L))
}
