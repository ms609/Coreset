% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{.DistColumn}
\alias{.DistColumn}
\title{Normalise a distance-column oracle result to a masked length-\code{N} vector}
\usage{
.DistColumn(colFn, i, N)
}
\arguments{
\item{colFn}{Column oracle; see \code{\link[=FarFirst]{FarFirst()}}.}

\item{i}{Integer 1-based index whose distance column is requested.}

\item{N}{Integer element count.}
}
\value{
\code{.DistColumn()} returns a numeric vector of length \code{N}, masked to \code{-Inf} at position \code{i}.
}
\description{
The user's \code{colFn(i)} may either report the self-distance (a length-\code{N}
vector, position \code{i} ignored) or omit it (a length-\code{N - 1} vector of the
distances to the other elements, in index order). Either way this returns a
length-\code{N} numeric vector with position \code{i} set to \code{-Inf}, so the downstream
\code{which.max()} / \code{pmin.int()} never re-select \code{i}. The mask invariant for the
oracle path lives here, not in the callers.
}
\keyword{internal}
