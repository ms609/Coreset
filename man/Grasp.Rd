% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grasp.R
\name{Grasp}
\alias{Grasp}
\title{GRASP with Path Relinking for the Max-Min Diversity Problem}
\usage{
Grasp(
  d,
  m,
  plateau = 100L,
  maxIter = NULL,
  eliteSize = 10L,
  alpha = 0.8,
  maxSeconds = Inf
)
}
\arguments{
\item{d}{Either a \code{dist} object or a square symmetric numeric matrix.}

\item{m}{Integer subset size, \verb{2 <= m <= nrow(d)}.}

\item{plateau}{Integer; stop after this many consecutive GRASP
iterations without an improvement to the best elite objective. The
primary, deterministic stopping criterion. Default 100.}

\item{maxIter}{Optional integer hard cap on GRASP refinement iterations
(excluding the elite-set construction). \code{NULL} (default) leaves
\code{plateau} in sole control.}

\item{eliteSize}{Size of the elite set |ES|. Default 10.}

\item{alpha}{RCL threshold; \code{alpha = 1} is pure greedy, \code{alpha = 0}
uniform random. Default 0.8.}

\item{maxSeconds}{Optional wall-clock ceiling in seconds. Default \code{Inf}
(no ceiling, fully reproducible). A finite value caps runtime but makes
the result machine-dependent.}
}
\value{
An integer vector of length \code{m} (1-based) \strong{sorted ascending}
(unlike \code{\link[=FarFirst]{FarFirst()}}, which returns farthest-first order)
with attributes:
\describe{
\item{score}{Achieved MaxMin objective \eqn{T_k}.}
\item{time_s}{Wall-clock seconds spent.}
\item{iters}{Number of GRASP refinement iterations executed.}
\item{pr_calls}{Number of path-relinking pair-applications run.}
}
The vector has class \code{"MaxMinSelection"} and prints as a one-line summary
(see \link{print.MaxMinSelection}); it is otherwise an ordinary integer vector.
}
\description{
Solves the Max-Min Diversity Problem (discrete p-dispersion) with the
GRASP / path-relinking metaheuristic of \insertCite{Resende2010;textual}{MaxMin},
static variant (their Fig. 4): a randomised-greedy construction
with extended-improvement local search builds and maintains an elite set,
followed by a single pass of path relinking over all elite pairs. On the
application benchmark this attains the highest \eqn{T_k} of the methods in
this package, at correspondingly higher cost.
}
\details{
\strong{Deterministic termination.} The refinement loop stops after
\code{plateau} consecutive GRASP iterations that fail to improve the best
elite objective (rather than after a wall-clock budget). Call \code{set.seed()}
before \code{Grasp()} for a reproducible run: the entire run — construction RNG,
iteration count, and result — is then reproducible and machine-independent,
because the compiled kernel draws from R's own session RNG stream. An
optional \code{maxSeconds} ceiling is available as a safety cap, but using a
finite value reintroduces machine-dependence and is off by default.

This is a \strong{dense-matrix-only} method: it materialises and repeatedly
subsets the full \eqn{n \times n} distance matrix, so it is suited to
instances small enough to hold that matrix. It offers no coordinate or
column-oracle path. For the matrix-free regime where the dense matrix is
infeasible, use \code{\link[=DropAdd]{DropAdd()}} (coordinate path via \code{points =}) or \code{\link[=FarFirst]{FarFirst()}} (coordinate or
distance-column oracle path), whose
\eqn{T_k} lands within roughly a percent on the benchmark while scaling to
far larger instances.
}
\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
# Call set.seed() before Grasp() for a reproducible run:
set.seed(1)
res <- Grasp(dist(pts), m = 5L, plateau = 20L, eliteSize = 4L)
res
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=DropAdd]{DropAdd()}} for scalable refinement;
\code{\link[=ExactMaxMin]{ExactMaxMin()}} for the proven optimum on small instances.
}
