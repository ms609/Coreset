% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grasp.R
\name{Grasp}
\alias{Grasp}
\title{GRASP with Path Relinking for the Max-Min Diversity Problem}
\usage{
Grasp(k, d, plateau = 100L, eliteSize = 10L, alpha = 0.8, maxSeconds = Inf)
}
\arguments{
\item{k}{Integer subset size, \verb{2 <= k <= nrow(d)}.}

\item{d}{Either a \code{dist} object or a square symmetric numeric matrix.}

\item{plateau}{Integer; stop after this many consecutive GRASP
iterations without an improvement to the best elite objective. The
primary, deterministic stopping criterion. Default 100.}

\item{eliteSize}{Size of the elite set |ES|. Default 10.}

\item{alpha}{RCL threshold; \code{alpha = 1} is pure greedy, \code{alpha = 0}
uniform random. Default 0.8.}

\item{maxSeconds}{Optional wall-clock ceiling in seconds. Default \code{Inf}
(no ceiling, fully reproducible). A finite value caps runtime but makes
the result machine-dependent.}
}
\value{
\code{Grasp()} returns an integer vector of length \code{k} (1-based) \strong{sorted ascending}
(unlike \code{\link[=FarFirst]{FarFirst()}}, which returns farthest-first order)
with attributes:
\describe{
\item{score}{Achieved MaxMin objective \eqn{T_k}.}
\item{time_s}{Wall-clock seconds spent.}
\item{iters}{Number of GRASP refinement iterations executed.}
\item{pr_calls}{Number of path-relinking pair-applications run.}
}
The vector has class \code{"MaxMinSelection"} and prints as a one-line summary
(see \code{\link[=print.MaxMinSelection]{print.MaxMinSelection()}}); it is otherwise an ordinary integer vector.
}
\description{
\code{Grasp()} solves the Max-Min Diversity Problem (discrete \emph{p}-dispersion) with
the static variant of the GRASP / path-relinking metaheuristic
\insertCite{@{}@Resende2010, fig. 4}{MaxMin}. This is the most expensive
heuristic in this package, and attains correspondingly high-quality
selections.
}
\details{
The GRASP with path-relinking algorithm conducts a randomised-greedy
construction with extended-improvement local search builds; it identifies an
elite set, then conducts a single pass of path relinking over all elite
pairs \insertCite{Resende2010}{MaxMin}.

The refinement loop stops after \code{plateau} consecutive GRASP iterations
fail to improve the best elite objective, or once \code{maxSeconds} have
elapsed.

This method will fail if the complete \eqn{N \times N} distance matrix, is
too large to fit into memory.
}
\section{Progress bar}{

In interactive sessions, a bar tracks how close the search is to its \code{plateau} stopping criterion, snapping back each time a better solution is found.
To toggle, set \code{options("MaxMin.progress" = FALSE)} (or \code{TRUE}).
}

\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
Grasp(5L, dist(pts), plateau = 20L, eliteSize = 4L)
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=DropAdd]{DropAdd()}} for scalable refinement;
\code{\link[=ExactMaxMin]{ExactMaxMin()}} for the proven optimum on small instances.
}
