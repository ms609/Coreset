% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polish.R
\name{PolishSelection}
\alias{PolishSelection}
\title{Local-search polish for a max-min diversity selection}
\usage{
PolishSelection(
  d,
  idx,
  limit = 20L,
  max_passes = 200L,
  progress = getOption("MaxMin.progress", interactive())
)
}
\arguments{
\item{d}{A \code{dist} object or square symmetric numeric matrix of pairwise
distances.}

\item{idx}{Integer vector of selected row/col indices (\code{k = length(idx)}).}

\item{limit}{Integer: maximum neighbour rank to scan per critical endpoint.
Default \code{20L}. Smaller values are faster but may miss improving swaps.}

\item{max_passes}{Integer: hard cap on outer iterations (safety guard;
theoretical termination is guaranteed by the lex-monotone objective).
Default \code{200L}.}

\item{progress}{Logical; show a start/done status line. Default: \code{TRUE} in
interactive sessions, \code{FALSE} otherwise
(\code{getOption("MaxMin.progress", interactive())}).}
}
\value{
Integer vector of the same length as \code{idx}, with attributes
\code{"passes"} and \code{"swaps"} (both equal to the number of accepted swaps).
}
\description{
Given a selection \code{idx} of size \code{k >= 2}, search for 1-swaps that increase
\eqn{T_k = \min_{a \neq b \in S} d(a, b)} (or, at fixed \code{T_k}, reduce the
number of pairs achieving it). The neighbourhood is critical-edge-anchored:
the candidate removal \code{x} ranges over endpoints of pairs achieving the
current minimum, and the candidate insertion \code{w} over the \code{limit} nearest
neighbours of \code{x} in the full data (excluding the current selection).
}
\details{
Acceptance follows Della Croce et al.'s flat-landscape rule: accept a swap
if \code{new_T > T} \emph{or} \code{new_T == T} \emph{and} the count of pairs at the minimum
strictly decreases. Both \code{T_k} and \code{-n_critical} are monotone non-decreasing
across accepted swaps, so iteration terminates without a tabu list.

The intended use is as a post-step on any greedy max-min selector — for
example \code{\link[=Gonzalez]{Gonzalez()}}. Calling \code{PolishSelection()} on a 1-swap local optimum
is a no-op.
}
\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d   <- dist(pts)
s0  <- Gonzalez(d, 5L, first = 1L)
s1  <- PolishSelection(d, s0)
TkScore(d, s1) >= TkScore(d, s0)
}
