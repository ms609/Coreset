% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coreset.R
\name{.FarFirstThin}
\alias{.FarFirstThin}
\title{Run a solver on a farthest-first coreset and map indices back}
\usage{
.FarFirstThin(k, m, d = NULL, points = NULL, RunOnSubset, label)
}
\arguments{
\item{k}{Integer: target subset size.}

\item{m}{Integer: coreset size (\verb{k <= m < N}, as returned by \code{\link[=.ResolveCap]{.ResolveCap()}}).}

\item{d}{Square distance matrix of the full problem, or \code{NULL} on the
coordinate path.}

\item{points}{\verb{N x dim} coordinate matrix of the full problem, or \code{NULL} on
the distance-matrix path. Exactly one of \code{d} / \code{points} is non-\code{NULL}.}

\item{RunOnSubset}{Function \verb{function(d, points)} that runs the downstream
solver on the restricted problem and returns its \code{MaxMinSelection}. It is
called with the restricted distance matrix (\code{d = d[core, core]}) on the
matrix path, or the restricted coordinates (\code{points = points[core, ]}) on
the coordinate path; the unused argument is \code{NULL}.}

\item{label}{Character naming the calling solver, used in the thinning
warning (e.g. \code{"DropAdd"}).}
}
\value{
\code{.FarFirstThin()} returns the solver's \code{MaxMinSelection} with its
indices mapped to original-space row indices (sorted ascending).
}
\description{
Implements the composable-coreset path of \code{\link[=DropAdd]{DropAdd()}} / \code{\link[=Grasp]{Grasp()}} (dispatched
there when their \code{maxCandidates} cap binds). It builds an \code{m}-point coreset
with \code{\link[=FarFirst]{FarFirst()}}, restricts the problem to those \code{m} points, runs the
supplied solver on the restriction, and maps the returned indices back to the
original numbering.
}
\details{
The coreset is built with the deterministic \code{"peripheral"} seed, so it draws
\strong{no} session RNG: it cannot perturb the random stream the downstream solver
(or the user) draws from, and it is bit-reproducible without \code{set.seed()} --
both required by the package's bit-identical-selection contract.

Only the integer index \emph{values} returned by the solver need remapping: the
\code{score} (and any other distance-derived attribute) is already valid in the
original space, because subsetting preserves pairwise distances. The result
is re-sorted ascending to keep the \code{\link[=DropAdd]{DropAdd()}} / \code{\link[=Grasp]{Grasp()}} sorted-output
contract, carrying the solver's class and attributes across unchanged.
}
\keyword{internal}
