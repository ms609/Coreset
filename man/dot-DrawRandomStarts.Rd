% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seed.R
\name{.DrawRandomStarts}
\alias{.DrawRandomStarts}
\title{Reproducible random start indices, isolated from ambient RNG}
\usage{
.DrawRandomStarts(N, k, seed = .kRandomSeed)
}
\arguments{
\item{N}{Integer element count (\verb{>= 1}).}

\item{k}{Integer number of pivots to draw.}

\item{seed}{Integer RNG seed; defaults to the package's fixed value.}
}
\value{
Integer vector of \code{min(k, N)} distinct indices.
}
\description{
Draws \code{min(k, N)} distinct indices in \verb{[1, N]} from a fixed internal seed,
the random pivots for the \code{"random_furthest"} seed. The caller's RNG kind and
\code{.Random.seed} are saved and restored, so the draw neither depends on nor
perturbs the ambient random stream; the result is therefore identical across
sessions and machines. Restoring the kind scrambles \code{.Random.seed}, so the
kind is restored before the seed.
}
\keyword{internal}
