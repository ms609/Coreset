% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kcentre.R
\name{ExactKCentre}
\alias{ExactKCentre}
\alias{ExactKCenter}
\title{Exact discrete k-centre optimum on small instances}
\usage{
ExactKCentre(
  k,
  d,
  solver = NULL,
  maxSeconds = 60,
  warmStart = NULL,
  progress = getOption("MaxMin.progress", interactive())
)

ExactKCenter(
  k,
  d,
  solver = NULL,
  maxSeconds = 60,
  warmStart = NULL,
  progress = getOption("MaxMin.progress", interactive())
)
}
\arguments{
\item{k}{Integer centre budget, \verb{1 <= k <= nrow(d)}.}

\item{d}{A \code{dist} object or a square symmetric numeric distance matrix.}

\item{solver}{Solver to use. Currently only \code{"highs"} is implemented; \code{NULL}
selects it.}

\item{maxSeconds}{Wall-clock budget in seconds for the whole search (shared
across the internal IP solves). If it expires before the optimum is proven,
the smallest radius proven feasible so far is returned with \code{proven = FALSE}.}

\item{warmStart}{Currently unused; reserved for a caller-supplied feasible
centre set. The internal CDSh warm start runs regardless.}

\item{progress}{Logical; show a progress indicator. Default: \code{TRUE} in
interactive sessions (\code{getOption("MaxMin.progress", interactive())}).}
}
\value{
\code{ExactKCentre()} returns a list of class \code{"KCentreExact"} with fields
\describe{
\item{indices}{Integer vector (ascending), length \verb{<= k}: the centres.}
\item{radius}{The covering radius they achieve; the proven optimum when
\code{proven} is \code{TRUE}, otherwise a valid upper bound.}
\item{proven}{Logical: \code{TRUE} if optimality was certified within budget.}
\item{time_s}{Wall-clock seconds elapsed.}
\item{solver}{Name of the MILP backend used.}
\item{n, k}{Instance size and centre budget.}
\item{n_centres}{\code{length(indices)}.}
}
It prints as a one-line summary and is otherwise an ordinary list.

The covering optimum may be attained by fewer than \code{k} centres (extra centres
never help once coverage is achieved); \code{indices} then has length \verb{< k} and the
reported \code{radius} is still the proven \emph{k}-centre optimum. The problem is
NP-hard, so this is an external ground-truth reference for small instances,
not a scalable method.
}
\description{
\code{ExactKCentre()} finds an optimal solution to the discrete \emph{k}-centre
problem.
}
\details{
The optimum covering radius is the smallest threshold \code{r}, over the achieved
distinct distances, for which \code{k} centres can cover every point within \code{r}.

Each probe solves a minimum-cardinality \emph{set-cover} integer program with
the \code{highs} MILP backend, the covering constraints held as a sparse matrix --
the covering dual of \code{\link[=ExactMaxMin]{ExactMaxMin()}}'s node-packing program. The search is
warm-started from the \code{\link[=KCentre]{KCentre()}} (CDSh) radius, a proven feasible upper bound
that caps the binary search, then bisects downward to the smallest feasible
radius.
}
\examples{
\donttest{
if (requireNamespace("highs", quietly = TRUE) &&
    requireNamespace("Matrix", quietly = TRUE)) {
  set.seed(1)
  pts <- matrix(rnorm(40), ncol = 2)
  d <- dist(pts)
  ExactKCentre(3L, d)
}
}
}
\references{
\insertAllCited{}
}
\seealso{
\code{\link[=KCentre]{KCentre()}} for the fast near-optimal heuristic; \code{\link[=ExactMaxMin]{ExactMaxMin()}} for
the dual MMDP optimum.
}
