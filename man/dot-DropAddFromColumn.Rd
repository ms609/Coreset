% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dropadd.R
\name{.DropAddFromColumn}
\alias{.DropAddFromColumn}
\title{DropAdd tabu search from a distance-column oracle}
\usage{
.DropAddFromColumn(
  colFn,
  N,
  k,
  first,
  plateau = 5000L,
  maxSeconds = Inf,
  maxIter = .Machine$integer.max,
  trace = FALSE
)
}
\arguments{
\item{colFn}{Column oracle; see \code{\link[=DropAdd]{DropAdd()}}.}

\item{N}{Integer element count.}

\item{k}{Integer subset size (\verb{2 <= k <= N}).}

\item{first}{Integer 1-based index of the seed element.}

\item{plateau}{Integer: stop after this many consecutive non-improving
iterations.}

\item{maxSeconds}{Numeric wall-clock budget. Checked once per iteration (an
iteration is dominated by two oracle calls, so \code{proc.time()} is free by
comparison and the 1024-iteration stride of the C++ kernel is unnecessary);
the check is skipped entirely when the budget is infinite, keeping the
default path deterministic.}

\item{maxIter}{Integer cap on main-loop iterations.}

\item{trace}{Logical: also return the dropped/added index sequences, for the
trajectory-identity tests.}
}
\value{
\code{.DropAddFromColumn()} returns a list with the same shape as
\code{DropAdd_cpp()}'s: \code{indices} (1-based, in FIFO buffer order), \code{objective},
\code{secondary}, \code{iters}, and -- when \code{trace} is \code{TRUE} -- \code{drops} and \code{adds}.
}
\description{
The pure-R counterpart of \code{DropAdd_cpp()}, substituting an on-demand
\code{colFn(i)} call for the matrix-column read \code{dmat[, i]}. Implements the
distance-column oracle path of \code{\link[=DropAdd]{DropAdd()}} (dispatched there when \code{d} is a
function); see that function's \emph{Distance-column oracle} section for the
user-facing contract. Every record update, tie-break and stopping rule
matches \code{src/dropadd.cpp} statement for statement, so on a symmetric oracle
the whole search trajectory -- not merely the final subset -- is identical to
the matrix path given the same seed.
}
\keyword{internal}
