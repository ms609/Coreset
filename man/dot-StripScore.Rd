% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gonzalez.R
\name{.StripScore}
\alias{.StripScore}
\title{Read and detach the kernel's free \code{t_k} score}
\usage{
.StripScore(idx)
}
\arguments{
\item{idx}{Integer vector returned by a maximin kernel.}
}
\value{
\code{idx} with its \code{t_k} attribute removed.
}
\description{
The maximin kernels attach the selection's minimum pairwise distance as a
\code{t_k} attribute (computed during the greedy pass at no extra cost). The
ensemble drivers read it via \code{\link[base:attr]{base::attr()}}; bare single passes strip it with
\code{\link[=.StripScore]{.StripScore()}} so the returned indices carry no incidental attribute.
}
\keyword{internal}
