% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dropadd.R
\name{.DropAddConstructColumn}
\alias{.DropAddConstructColumn}
\title{Constructive phase of DropAdd from a distance-column oracle}
\usage{
.DropAddConstructColumn(colFn, N, k, first)
}
\arguments{
\item{colFn}{Column oracle; see \code{\link[=DropAdd]{DropAdd()}}.}

\item{N}{Integer element count.}

\item{k}{Integer subset size (\verb{2 <= k <= N}).}

\item{first}{Integer 1-based index of the seed element.}
}
\value{
\code{.DropAddConstructColumn()} returns a \code{new.env(parent = emptyenv())}
holding \code{S} (the selection, in add order), \code{inS}, \code{minDist}, \code{sumDist} and
\code{minDistCount}, matching the record set of \code{.DropAddConstruct()}.
}
\description{
Algorithm 1 of \insertCite{Porumbel2011;textual}{MaxMin} against a column
oracle: the counterpart of \code{.DropAddConstruct()} (and of the construction
block of \code{src/dropadd.cpp}) that never materialises the distance matrix.
Costs one oracle call per selected element.
}
\section{Seed deviation}{

The matrix kernel seeds at the max-row-sum point,
\eqn{\mathrm{argmax}_x \sum_y d(x, y)}. From a column oracle that needs all
\code{N} columns -- the full \eqn{O(N^2)} pairwise set, the very cost this path
exists to avoid. We therefore substitute the \eqn{O(N)} two-sweep peripheral
point of \code{\link[=.PeripheralSeedColumn]{.PeripheralSeedColumn()}} (the element furthest from element 1, then
the element furthest from that; two oracle calls, no RNG). This is the
column-oracle twin of the centroid-peripheral seed substituted on the
coordinate path (\code{src/dropadd_mf.cpp}), and rests on the same argument: a
peripheral element has a large total distance to the rest, so it tracks the
max-row-sum point closely. The seed has little bearing on the converged
objective in any case -- the greedy construction is a 2-approximation from
\emph{any} start, and the drop-add tabu search determines the result. Ties break
to the smallest index, as in the matrix kernel, so when the two seed rules
coincide the whole trajectory matches the matrix path bit-for-bit. Pass
\code{DropAdd(seed=)} to override.
}

\references{
\insertAllCited{}
}
\keyword{internal}
