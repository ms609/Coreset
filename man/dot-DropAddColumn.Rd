% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dropadd.R
\name{.DropAddColumn}
\alias{.DropAddColumn}
\title{Normalise a distance-column oracle result for the DropAdd records}
\usage{
.DropAddColumn(colFn, i, N)
}
\arguments{
\item{colFn}{Column oracle; see \code{\link[=FarFirst]{FarFirst()}}.}

\item{i}{Integer 1-based index whose distance column is requested.}

\item{N}{Integer element count.}
}
\value{
\code{.DropAddColumn()} returns a numeric vector of length \code{N} whose
position \code{i} is \code{0}, matching a distance matrix's diagonal.
}
\description{
\code{\link[=.DistColumn]{.DistColumn()}} masks the self-distance to \code{-Inf}, which is what
\code{which.max()} / \code{pmin.int()} want on \code{\link[=FarFirst]{FarFirst()}}'s oracle path. DropAdd's
streamlined records want the opposite: \code{src/dropadd.cpp} reads a matrix whose
diagonal is \code{0} and folds the \emph{whole} column into \code{sumDist} (relying on
\code{d(x, x) = 0} being harmless), masking \code{minDist} at the selected index
separately. A \code{-Inf} self entry would poison \code{sumDist} irrecoverably and
corrupt the MMDPo tie-break while leaving the returned indices plausible. So
take \code{\link[=.DistColumn]{.DistColumn()}}'s length normalisation and \code{NA} guard, then restore the
zero diagonal.
}
\keyword{internal}
