% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{.MinPairwiseFromPoints}
\alias{.MinPairwiseFromPoints}
\title{Minimum pairwise distance within a selection, from coordinates}
\usage{
.MinPairwiseFromPoints(points, idx)
}
\arguments{
\item{points}{A \code{double} \verb{N x dim} coordinate matrix.}

\item{idx}{Integer indices of the selection.}
}
\value{
\code{.MinPairwiseFromPoints()} returns a numeric scalar; \code{NA_real_} if \code{length(idx) < 2}.
}
\description{
Coordinate counterpart of \code{.SubsetScore(d, idx, "min_pairwise")}. Computes
\code{stats::dist()} on the selected sub-coordinates only (\verb{k x k}, never the
full matrix); the per-pair bits are identical to the corresponding entries
of the full distance matrix, so the returned scalar matches the matrix path.
}
\keyword{internal}
