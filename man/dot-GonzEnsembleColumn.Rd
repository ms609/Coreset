% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/farfirst.R
\name{.GonzEnsembleColumn}
\alias{.GonzEnsembleColumn}
\title{Seed ensemble from a distance-column oracle}
\usage{
.GonzEnsembleColumn(colFn, N, k, anchors, nSeeds, progress = FALSE)
}
\arguments{
\item{colFn}{A function that, when passed an index \code{i}, must return a
vector of distances from element \code{i} to either (i) every element in turn,
including \code{i}; or (ii) every other element. See \code{\link[=.DistColumn]{.DistColumn()}}.}

\item{N}{Integer: the total number of elements.}

\item{k}{Integer: number of elements to select. If \code{k > N}, all \code{N} indices
are returned in Gonzalez (farthest-first) order.}

\item{anchors}{Any of \code{"random_furthest"}, \code{"peripheral"} and start indices
as strings (see \code{\link[=.NormaliseStrategy]{.NormaliseStrategy()}}).}

\item{nSeeds}{Integer number of distinct random-furthest seeds.}

\item{progress}{Logical; show a progress bar during greedy selection.}
}
\value{
\code{.GonzEnsembleColumn()} returns an integer vector of selected
indices with the attributes of \code{\link[=.ResolveEnsemble]{.ResolveEnsemble()}}.
}
\description{
The distance-column counterpart of \code{\link[=.GonzEnsemble]{.GonzEnsemble()}}, over the anchors an
oracle can reach: start indices, the peripheral seed, and \code{nSeeds} distinct
random-furthest seeds, drawn exactly as the matrix path draws them. Each
distinct seed runs one \code{\link[=.GonzalezColumn]{.GonzalezColumn()}} pass, serially.
}
\keyword{internal}
