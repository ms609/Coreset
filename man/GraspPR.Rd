% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grasp.R
\name{GraspPR}
\alias{GraspPR}
\title{GRASP with Path Relinking for the Max-Min Diversity Problem}
\usage{
GraspPR(
  d,
  m,
  max_no_improve = 100L,
  max_iter = NULL,
  elite_size = 10L,
  alpha = 0.8,
  time_budget_s = Inf,
  seed = NULL
)
}
\arguments{
\item{d}{Either a \code{dist} object or a square symmetric numeric matrix.}

\item{m}{Integer subset size, \verb{2 <= m <= nrow(d)}.}

\item{max_no_improve}{Integer; stop after this many consecutive GRASP
iterations without an improvement to the best elite objective. The
primary, deterministic stopping criterion. Default 100.}

\item{max_iter}{Optional integer hard cap on GRASP refinement iterations
(excluding the elite-set construction). \code{NULL} (default) leaves
\code{max_no_improve} in sole control.}

\item{elite_size}{Size of the elite set |ES|. Default 10.}

\item{alpha}{RCL threshold; \code{alpha = 1} is pure greedy, \code{alpha = 0}
uniform random. Default 0.8.}

\item{time_budget_s}{Optional wall-clock ceiling in seconds. Default \code{Inf}
(no ceiling, fully reproducible). A finite value caps runtime but makes
the result machine-dependent.}

\item{seed}{Optional integer; if supplied, \code{set.seed(seed)} is called at
entry. \code{GraspPR} is genuinely stochastic (randomised construction and RCL
sampling), so the seed governs the trajectory and the returned selection.}
}
\value{
A list with elements
\describe{
\item{indices}{Integer vector of length \code{m}, 1-based, sorted ascending.}
\item{objective}{Achieved MaxMin objective \eqn{T_k}.}
\item{time_s}{Wall-clock seconds spent.}
\item{iters}{Number of GRASP refinement iterations executed.}
\item{pr_calls}{Number of path-relinking pair-applications run.}
}
}
\description{
Solves the Max-Min Diversity Problem (discrete p-dispersion) with the
GRASP / path-relinking metaheuristic of Resende, Marti, Gallego & Duarte
(2010), static variant (their Fig. 4): a randomised-greedy construction
with extended-improvement local search builds and maintains an elite set,
followed by a single pass of path relinking over all elite pairs. On the
application benchmark this attains the highest \eqn{T_k} of the methods in
this package, at correspondingly higher cost.
}
\details{
\strong{Deterministic termination.} The refinement loop stops after
\code{max_no_improve} consecutive GRASP iterations that fail to improve the best
elite objective (rather than after a wall-clock budget). Given a fixed
\code{seed}, the entire run — construction RNG, iteration count, and result — is
therefore reproducible and machine-independent; \code{set.seed(seed)} controls
the same random stream the compiled kernel consumes via R's RNG. An
optional \code{time_budget_s} ceiling is available as a safety cap, but using a
finite value reintroduces machine-dependence and is off by default.

This is a \strong{dense-matrix-only} method: it materialises and repeatedly
subsets the full \eqn{n \times n} distance matrix, so it is suited to
instances small enough to hold that matrix. It offers no coordinate or
column-oracle path. For the matrix-free regime where the dense matrix is
infeasible, use \code{\link[=DropAddTSPoints]{DropAddTSPoints()}} or \code{\link[=GonzalezColumn]{GonzalezColumn()}}, whose
\eqn{T_k} lands within roughly a percent on the benchmark while scaling to
far larger instances.
}
\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
res <- GraspPR(dist(pts), m = 5L, max_no_improve = 20L, elite_size = 4L,
               seed = 1L)
res$indices
}
\references{
Resende MGC, Marti R, Gallego M, Duarte A (2010). GRASP and path relinking
for the max-min diversity problem. \emph{Computers & Operations Research}
37(3):498-508. \doi{10.1016/j.cor.2008.05.011}
}
\seealso{
\code{\link[=DropAddTS]{DropAddTS()}} and \code{\link[=DropAddTSPoints]{DropAddTSPoints()}} for scalable refinement;
\code{\link[=ExactMaxMin]{ExactMaxMin()}} for the proven optimum on small instances.
}
