% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seed.R
\name{DrawDistinctSeeds}
\alias{DrawDistinctSeeds}
\title{Distinct random-furthest seeds}
\usage{
DrawDistinctSeeds(
  d = NULL,
  points = NULL,
  N = NULL,
  nSeeds = 3L,
  maxDraws = NULL
)
}
\arguments{
\item{d}{A \code{dist} object, a square numeric matrix of pairwise distances, or
a distance function that takes an index \code{i} and returns the distance from
\code{i} to each other element (optionally including the self-distance).
Ignored when \code{points} is supplied.}

\item{points}{Optional \verb{N x dim} numeric coordinate matrix. When supplied,
the selection is computed directly from coordinates in
\eqn{O(N \cdot k \cdot dim)} time and \eqn{O(N)} memory, which avoids
creating an \eqn{O(N^2)} distance matrix. Missing entries (\code{NA}) are not
supported.}

\item{N}{Integer: the total number of elements. Required (and used) only on
the distance-column oracle path, where it cannot be inferred from the
closure; ignored for the matrix and coordinate paths.}

\item{nSeeds}{Integer: the number of distinct seeds to draw.}

\item{maxDraws}{Integer: the most pivots to try before returning however many
distinct seeds have been found. The default, \code{max(40 * nSeeds, 100)},
bounds the search when the reachable pool is smaller than \code{nSeeds}. At
most \code{N} pivots are ever tried.}
}
\value{
\code{DrawDistinctSeeds()} returns an integer vector of between 1 and
\code{nSeeds} distinct element indices, in ascending order rather than the
order in which they were drawn. Pass each to \code{\link[=FarFirst]{FarFirst()}} as \code{strategy}.
}
\description{
\code{DrawDistinctSeeds()} draws the seeds that the \code{"random_furthest"} strategy
of \code{\link[=FarFirst]{FarFirst()}} starts from, so that a restart can be assembled from
separate farthest-first passes. This is needed where \code{FarFirst()} cannot
draw the seeds itself: when distances are supplied one column at a time,
\code{FarFirst()} honours only an explicit start index.
}
\details{
A random-furthest seed is the element furthest from a random pivot. Pivots
are visited in a random order without replacement; each pivot's furthest
element is kept if it has not already been found, until \code{nSeeds} distinct
seeds are found or \code{maxDraws} pivots have been tried. Distinct pivots often
share a furthest element -- on low-dimensional data a handful of extreme
elements are furthest from almost everything -- so the pool of reachable
seeds can be smaller than \code{nSeeds}, and fewer seeds are then returned.

Each pivot costs one column of distances: \eqn{O(N)} distance evaluations
from \code{d}, or \eqn{O(N \cdot dim)}{O(N * dim)} arithmetic from \code{points}.

The draw uses R's random number generator, and consumes it exactly as
\code{FarFirst(strategy = "random_furthest", nSeeds = nSeeds)} does, so from the
same \code{set.seed()} state both use the same seeds.
}
\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d <- dist(pts)

set.seed(2)
seeds <- DrawDistinctSeeds(d, nSeeds = 3)
seeds

# One farthest-first pass from each seed, keeping the best, reproduces
# the default strategy of FarFirst():
passes <- lapply(seeds, function(s) FarFirst(5, d, strategy = s))
best <- passes[[which.max(vapply(passes, attr, numeric(1), "score"))]]
set.seed(2)
attr(FarFirst(5, d, nSeeds = 3), "score") == attr(best, "score")

# The same restart where distances are computed one column at a time:
Column <- function(i) sqrt(colSums((t(pts) - pts[i, ]) ^ 2))
set.seed(2)
colSeeds <- DrawDistinctSeeds(Column, N = nrow(pts), nSeeds = 3)
identical(colSeeds, seeds)
}
\seealso{
\code{\link[=FarFirst]{FarFirst()}}, which draws these seeds and runs the passes in one
call where the full matrix or coordinates are available; \code{\link[=PickPoint]{PickPoint()}}
for a single seed.
}
