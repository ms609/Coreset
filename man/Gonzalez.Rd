% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gonzalez.R
\name{Gonzalez}
\alias{Gonzalez}
\title{Deterministic Gonzalez furthest-point selection}
\usage{
Gonzalez(
  d = NULL,
  n,
  seed = .kDefaultEnsemble,
  pivots = NULL,
  points = NULL,
  N = NULL,
  progress = getOption("MaxMin.progress", interactive())
)
}
\arguments{
\item{d}{A \code{dist} object, a square symmetric numeric matrix of pairwise
distances, or a \strong{distance-column oracle} function (see
\emph{Distance-column oracle}). Ignored when \code{points} is supplied.}

\item{n}{Integer: number of points to select. If \code{n >= N}, all
indices are returned.}

\item{seed}{Integer or character (scalar or vector). An \strong{integer} gives the
explicit 1-based index of the first selected point (a single bare Gonzalez
pass). A \strong{length-1 character} names a single deterministic seeding
strategy run as one bare pass: \code{"centroid"} (coordinates only),
\code{"peripheral"} (two-sweep diameter-endpoint approximation), \code{"diameter"},
\code{"anti_medoid"}, \code{"medoid"}, \code{"rowsum"}, \code{"rownorm"}, or \code{"first"}
(index 1). A \strong{length > 1 character vector} -- or the lone
\code{"random_furthest"} token -- requests an ensemble: each named anchor runs a
full Gonzalez pass and the best result by \code{\link[=MinDist]{MinDist()}} is returned with
\code{strategy_results} and \code{winning_strategy} (character vector of all
tied-best strategies) attributes. The \code{"random_furthest"} token expands to
one start per element of \code{pivots}, labelled \code{random_furthest1},
\code{random_furthest2}, ...; named on its own it still runs the ensemble (one
pass per pivot), so a single random start is best obtained via
\code{\link[=MaxMinSeed]{MaxMinSeed()}}. Valid ensemble anchors: any subset of \code{c("centroid", "peripheral", "random_furthest", "diameter", "anti_medoid", "medoid", "rowsum", "rownorm")} (\code{"centroid"} requires \code{points}). Default:
\code{"random_furthest"} (three random starts; see \code{pivots}). See \code{\link[=MaxMinSeed]{MaxMinSeed()}}
for anchor definitions. On the distance-column oracle path only an integer
\code{seed} is honoured; a named or ensemble \code{seed} there warns and falls back
to the peripheral seed (see \emph{Distance-column oracle}).}

\item{pivots}{Integer vector of pivot indices over which the
\code{"random_furthest"} ensemble token expands: each pivot contributes one
start, seeded at the point furthest from it, so the vector's length sets
the number of random-furthest starts. Left unspecified, three pivots are
drawn with the session RNG (\code{sample.int(N, 3)}; set a seed for a
reproducible selection). Pass \code{integer(0)}, \code{NA}, or \code{NULL} to disable the
random starts, or an index vector to choose the pivots (and their count)
explicitly. Disabling the random starts errors under the default \code{seed}
(which names only \code{"random_furthest"}, leaving no anchor); pair it with a
deterministic \code{seed} such as \code{"peripheral"}.}

\item{points}{Optional \verb{N x dim} numeric coordinate matrix. When supplied,
the selection is computed directly from coordinates in \code{O(N * n * dim)}
time and \code{O(N)} memory, never materialising the \verb{N x N} distance matrix
(\code{d} is then unused). For Euclidean data the returned indices are
identical to the matrix path. Only complete (non-\code{NA}) data is supported.}

\item{N}{Integer: the total number of elements. Required (and used) only on
the distance-column oracle path, where it cannot be inferred from the
closure; ignored for the matrix and coordinate paths.}

\item{progress}{Logical; show a progress bar during greedy selection on the
distance-column oracle path (the only path slow enough to warrant one).
Default: \code{TRUE} in interactive sessions, \code{FALSE} otherwise
(\code{getOption("MaxMin.progress", interactive())}).}
}
\value{
Integer vector of length \code{min(n, N)} of selected indices.
}
\description{
Greedy k-centre selection (Gonzalez 1985). Iteratively selects the point
furthest from the current selection, a 2-approximation to the k-centre
problem. The quality of the result depends on the first (seed) point; by
default \code{Gonzalez()} runs an \strong{ensemble} of cheap \code{O(N)} seeding strategies
and keeps the selection with the largest minimum pairwise distance
(\code{\link[=MinDist]{MinDist()}}). The default ensemble is three \code{"random_furthest"} starts -- a
best-of-three selection. The random starts use the session RNG, so set a seed
(\code{set.seed()}) for a reproducible selection. The deterministic \code{O(N)} anchors
(\code{"centroid"}, \code{"peripheral"}) and the costlier \code{O(N^2)} anchors
(\code{"diameter"}, \code{"anti_medoid"}, \code{"medoid"}, \code{"rowsum"}, \code{"rownorm"}) are
available as opt-in \code{seed} strategies.
}
\details{
\code{Gonzalez()} accepts the distances in whichever of three forms suits the
data, all returning identical selections on the same metric:
\describe{
\item{a \strong{distance matrix} (\code{d})}{a \code{dist} object or square matrix, held
in full;}
\item{a \strong{coordinate matrix} (\code{points})}{each needed distance is
recomputed from coordinates on the fly in \code{O(N)} memory, never
materialising the \verb{N x N} matrix (Euclidean data only);}
\item{a \strong{distance-column oracle} (a function passed as \code{d})}{for metrics
with neither a stored matrix nor a coordinate embedding -- e.g.
tree-to-tree distances computed on demand. See \emph{Distance-column oracle}.}
}
}
\section{Distance-column oracle}{

When \code{d} is a function it is treated as a closure \code{colFn(i)} returning, for a
single 1-based index \code{i}, the length-\code{N} vector of distances from element \code{i}
to every element. Gonzalez calls it once per selected element, maintaining a
running nearest-distance vector, so the \verb{N x N} matrix is never built:
\code{O(N * n)} oracle calls and \code{O(N)} memory. The self-distance at position \code{i}
may take any non-negative value; it is masked before use. Because the count
of elements cannot be inferred from the closure, \code{N} must be supplied.
Only an integer \code{seed} (a \code{first} index) or a single deterministic two-sweep
peripheral seed is reachable from an oracle; the other anchors need either
coordinates or \code{O(N^2)} work. An explicitly named or ensemble \code{seed} on this
path is ignored with a warning and the peripheral seed is used instead.
}

\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d <- dist(pts)
# Default: best of three random-furthest starts (set.seed for reproducibility):
Gonzalez(d, 5L)
# More random-furthest starts (length of `pivots` sets the count):
Gonzalez(d, 5L, pivots = sample.int(nrow(as.matrix(d)), 8L))
# Or choose the pivots explicitly:
Gonzalez(d, 5L, pivots = c(1L, 10L, 20L))
# Custom two-anchor ensemble:
Gonzalez(d, 5L, seed = c("diameter", "anti_medoid"))
# A single strategy:
Gonzalez(d, 5L, seed = "diameter")
# An explicit start index (integer seed):
Gonzalez(d, 5L, seed = 1L)
# Matrix-free coordinate path (identical result, O(N) memory):
Gonzalez(n = 5L, points = pts, seed = 1L)
# Distance-column oracle: supply one column at a time, never the full matrix.
dm <- as.matrix(d)
colFn <- function(i) dm[, i]
identical(Gonzalez(colFn, 5L, N = nrow(dm), seed = 1L),
          Gonzalez(d, 5L, seed = 1L))
}
\seealso{
\code{\link[=MaxMinSeed]{MaxMinSeed()}} for the seed indices alone; \code{\link[=DropAddTS]{DropAddTS()}} and
\code{\link[=ExactMaxMin]{ExactMaxMin()}} for higher-effort solvers.
}
