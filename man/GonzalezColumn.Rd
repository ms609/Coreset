% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gonzalez.R
\name{GonzalezColumn}
\alias{GonzalezColumn}
\title{Gonzalez maximin from a distance-column oracle (matrix-free, any metric)}
\usage{
GonzalezColumn(
  colFn,
  N,
  n,
  first = NULL,
  progress = getOption("MaxMin.progress", interactive())
)
}
\arguments{
\item{colFn}{A function of a single 1-based index \code{i} returning a length-\code{N}
numeric vector of distances from element \code{i} to every element. The
self-distance at position \code{i} may take any non-negative value; it is
masked before use.}

\item{N}{Integer: the total number of elements. It cannot be inferred from
\code{colFn}, so it must be supplied.}

\item{n}{Integer: number of elements to select. If \code{n >= N}, all indices are
returned.}

\item{first}{Integer index of the first selected element, or \code{NULL}
(default) to use a deterministic peripheral seed computed from two oracle
sweeps: the element furthest from element 1, then the element furthest
from that (a diameter-endpoint approximation).}

\item{progress}{Logical; show a progress bar during greedy selection.
Default: \code{TRUE} in interactive sessions, \code{FALSE} otherwise
(\code{getOption("MaxMin.progress", interactive())}).}
}
\value{
Integer vector of length \code{min(n, N)} of selected indices.
}
\description{
Closure-driven counterpart of \code{\link[=Gonzalez]{Gonzalez()}} for spaces with no coordinate
embedding (e.g. phylogenetic trees), where neither the matrix path nor the
Euclidean coordinate path applies. At each greedy step the distances from
the newly selected element to all \code{N} elements are obtained from \code{colFn},
and a running nearest-distance vector is maintained. The \verb{N x N} distance
matrix is never materialised: \code{O(N * n)} oracle calls and \code{O(N)} memory.
}
\details{
On a symmetric metric the selection is identical to
\code{Gonzalez(d, n, seed = first)} given the same starting index, because
\code{colFn(i)} returns column \code{i} of the distance matrix.

Only the deterministic two-sweep peripheral seed is available here: the
richer anchors of \code{\link[=Gonzalez]{Gonzalez()}} (diameter, anti-medoid, row-sum, row-norm)
need \code{O(N^2)} work and are unreachable from an \code{O(N)}-per-call oracle.
}
\examples{
set.seed(1)
pts <- matrix(rnorm(60), ncol = 2)
d <- as.matrix(dist(pts))
colFn <- function(i) d[, i]
identical(GonzalezColumn(colFn, nrow(d), 5L, first = 1L),
          Gonzalez(d, 5L, seed = 1L))
}
\seealso{
\code{\link[=Gonzalez]{Gonzalez()}} for the matrix and coordinate paths.
}
