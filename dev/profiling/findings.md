# Profiling findings — MaxMin

Task-row format. Kinds: `[Port]` (move R work to C++), `[Optimise]` (compiled-code
fix), `[AT-LIMIT]` (at hardware ceiling, skip henceforth).

| ID | Pri | State | Area | Finding | Evidence |
|----|-----|-------|------|---------|----------|
| T-001 | P1 | DONE | FarFirst | `[Optimise]` Kernel returns T_k for free (min insertion distance during the greedy pass); ensemble reads the `t_k` attribute instead of re-running `MinDist`/`stats::dist` over the selection. | `MinDist` re-scoring was 30–78% of wall time at n/N ≥ 0.2 (verify.R). Removing it: matrix **4.03×** and points **2.18×** at n/N=0.5; free-T_k bit-identical to MinDist (max err 0.000e+00, 1620/1620 cases). |
| T-002 | P1 | DONE | FarFirst | `[Optimise]` Points greedy pass runs in **squared-distance space** (drop per-element `sqrt`; one final sqrt for T_k). which.max/pmin are monotone under sqrt, so selection is unchanged. | ~1.2× in the narrow regime (pass-bound), compounding to 2.44× at n/N=0.5 (points, dim=10). Cross-path identity preserved 1620/1620; all 88 test-gonzalez + 45 test-seed-score assertions green. |
| T-003 | — | NOTE | FarFirst | `[Port]` hypothesis **REJECTED**: folding the ensemble into one C++ call to "cross the R↔C++ barrier once" buys ~nothing. | Seed-find + all R wrangling = **< 0.2 ms total regardless of n** — noise against O(N·n·dim) compute. ~9 barrier crossings each carry an O(N) payload; per-call overhead is not the cost. The win was redundant *work* (T-001) and inner-loop *sqrt* (T-002), not crossings. |
| T-004 | P2 | DONE→AT-LIMIT | FarFirst | `[AT-LIMIT]` BLAS-gemv (Gram) does **not apply here**: R links the reference BLAS (`extSoftVersion()[["BLAS"]]` empty), so `dgemv` would not beat the scalar loop, and Gram risks cancellation. Tried instead a **bit-identical column-sequential** rewrite of the hot column (`EuclidColSqInto`: one contiguous SIMD pass per dim into a reused buffer, vs `dim` strided streams per point). Measured: **1.06–1.08× at dim=2, 0.99–1.01× at dim=10** — within noise; the pass is at its practical limit after T-001/T-002 (L2-resident, already vectorised). Kept (consistent small low-dim win, bit-identical, no added complexity); area marked AT-LIMIT. | Verified vs prior build: FarFirst points 128/128 bit-identical. Timing (single pass, N=6000): d2 n300 4.47→4.15, n1500 21.6→20.3, n3000 42.6→40.4; d10 ~flat. |

| T-005 | P1 | DONE (a) | DropAdd | `[Optimise]` **(a) IMPLEMENTED — matrix-path seed column-sequential rewrite: verified 11.3× (n=4000) / 13.1× (n=6000)** on construction (116→10 ms, 293→22 ms; maxIter=0). DropAdd results 96/96 bit-identical (matrix+points). The cache-hostile strided row-sum diagnosis below was correct; **(a) matrix-path seed (`dropadd.cpp` 50–58) is cache-hostile, not algorithmically reducible:** `rs += D(i,j)` with `D(i,j)=dp[i+j*n]` walks the column-major matrix in row-major order (stride n·8 B → a miss per step). The seed value `argmax_i Σ_j D(i,j)` is the Porumbel definition and must NOT change — but accumulating row-sums **column-sequentially** (`for j: for i: rs[i]+=col[i]`) gives bit-identical sums with sequential access; expect ~5–10× on the seed (115 ms is ~9× over the bandwidth floor at n=4000). NOTE: the earlier "replace with anti_centroid proxy" idea is WRONG — it changes the result and the matrix path has no coordinates. **(b) points-path construction is O(m·n·dim)** — inherent to building an m-point on-the-fly greedy selection; at n=20000 m=10000 it is 1060–1880 ms. **AT-LIMIT: the squared-space trick (T-002) does NOT apply here** — DropAdd's secondary objective `sum_dist` accumulates *real* Euclidean distances (dropadd_mf.cpp:113,173) and is both summed into the reported objective (`best_score`, :178) and the ADD argmax tie-break (:128–136); running in squared space would change which subset is selected and the objective reported (a wrong answer, not a precision wobble). The `sqrt` is load-bearing. The only mechanically-available change is the same `EuclidColSqInto` cache-reorder T-004 measured at 1.06–1.08× (dim=2) / ~1.0× (dim=10) on a pure pass — and here it dilutes below the 5% floor against the O(n) record-update passes. Not pursued. Search per-iter is flat in m (two O(n·dim) column passes dominate; recompute branch negligible). | `maxIter=0` subtraction: matrix n=4000 m=10 construct=115 ms vs search=45 ms (1500 iters); points n=20000 dim=2 m=10000 construct=1060 ms vs search=390 ms. Search per-iter flat: dim=2 0.23 ms, dim=10 0.45–0.55 ms, independent of m. profvis: 98.8–100% in the C++ kernels, no R target. |

| T-006 | P1 | DONE | Grasp | `[Optimise]` **IMPLEMENTED — verified 14.2× (m=50) / 15.6× (m=100)** (n=200: 1329→85 ms). Grasp results 192/192 bit-identical. The O(m²) sub-matrix min-rescore in `Grasp_cpp`'s path-relinking walk (phase C, 93.6%) and local-search swap scan (phase B) is replaced by a hoist: for a fixed drop, `min-pair-within(rem)` is constant across all adds → compute once per drop; each add is then O(m) via `min_to_set`. Bit-identical because `min` is exact (the candidate min = min(base, cross)). The LS extended-improvement pair-count is deferred to only the candidates that can win (nd ≥ best). Original finding evidence below. | maxIter=0 at n=200/m=100/eliteSize=5: phase A+C = 2070 ms vs full run 2160 ms → phase B = 90 ms (4.1%), phase C = 2020 ms (93.6%), phase A = 50 ms (2.3%). eliteSize=1/maxIter=0 = 10 ms (single construct+LS). PR cost per call: 85–90 ms across eliteSize 3→5→8 (linear in pr_calls). At m=10 all phases trivial (~5.5 ms total). |
| T-007 | P1 | DONE | Grasp | `[Optimise]` **IMPLEMENTED — verified 1.34× (m=50) / 2.07× (m=100)** (n=200: 21.9→16.4 / 81.9→39.5 ms). Grasp results 192/192 bit-identical (416/416 over the whole battery). Second-order hoist beyond T-006: after T-006, `base_z = objective_of(pk \ {di_})` (the post-drop min-pair-within) was **still recomputed O(m²) per drop candidate** in both `grasp_path_relink` (`grasp.cpp:245`) and its local-search twin (`:159`) → O(\|drop_cands\|·m²) per step. New `min_edge_witness` computes the global min edge of the set **once per step** with a witness `(wa,wb)`; dropping any vertex ≠ {wa,wb} leaves that edge intact ⇒ `base_z == gmin` in O(1), and only the ≤2 witness vertices rescore. Per-step base_z falls O(\|drop_cands\|·m²) → O(m²). **Bit-identical**: `min` re-selects the identical surviving `D()` value (same matrix cell, sel ascending). Size guard `rem.size()<2 → POS_INF` kept first (tiny-m branch). Win grows with m (PR phase = 93.6%); holds even at worst-case r=m (~2.3–2.5× on the PR walk). | `verify-r2.R` old-vs-new: corr 416/416 identical (gp 192/192); timing grasp_n200_m50 21.92→16.35 ms (1.34×), grasp_n200_m100 81.90→39.52 ms (2.07×); all FarFirst/DropAdd timing flat (≤1.03×, untouched). |

| T-008 | P1 | DONE | ExactMaxMin | `[Optimise]` **IMPLEMENTED — verified 19.6× over the exact grid (222.6→11.3 s), 56/56 proven optima bit-identical to the dense solver.** Two stacked levers in `R/exact.R`: **(a) sparse packing matrix** — the node-packing constraint matrix (2 nnz/edge) was built dense (`matrix(0, nEdge, n)`), O(nEdge·n) ≈ 1.9–3.9 GB/probe at n=683–990 and the real reason the solver stalled at n≈240; rebuilt as a `Matrix::sparseMatrix` (highs accepts it, bit-identical result). **(b) heuristic warm-start gallop** — instead of bisecting all ~n²/2 distinct distances (~16 IP solves, half splitting hairs at the optimum), seed a provably-achievable lower bound from the package's own heuristics (best of 8 `Grasp` restarts + `DropAdd`), then gallop up to the first infeasible threshold and bisect the small bracket. When a heuristic attains the optimum (55/56 ground-truth (case,k); Grasp hits it where DropAdd misses) a **single** infeasibility solve certifies it; k=2 needs **zero** solves (heuristic = diameter). Correctness is independent of seed quality — the warm start only sets the starting lower bound, the search proves the true optimum regardless (the 1 heuristic miss, tc6 k=10, still returned exact in 9 probes). Also precomputes the upper-tri edge list once (was a fresh n×n `which(... & upper.tri, arr.ind)` per probe, ~933 MB). `set.seed()` governs the returned witness (objective is RNG-independent); `Matrix` added to Suggests. **REJECTED en route:** a feasibility reformulation (constant objective + `sum(x)≥m` cardinality cut, stop at first feasible) was **slower** (0.73–0.89×) — the cut weakens the LP relaxation and removes the objective guidance highs prunes with; `objective_bound` cutoff had no effect under maximise. | Rprof (penguins n=342 k=10): `solver_run` (highs) 86% self, `which(...arr.ind)` 7.4%/933 MB, dense `matrix` 4.4%/176 MB. Per-probe instrumentation: ~16 evenly-costed probes (~0.4 s penguins / ~0.9 s ionosphere), half wasted near the optimum. `verify_new.R` vs `oldopt.rds`: 56/56 proven & equal; OLD 222.6 s → NEW 11.3 s (19.6×), per-case median 16.3×; brute-force oracle (test-exact.R) + full suite 598 PASS / 0 FAIL; RNG: objective seed-independent, selection seed-reproducible. |

| T-010 | P1 | DONE | KCentre (CDSh) | `[Optimise]` **IMPLEMENTED — verified 4.16× (1275→307 ms/call, n=2004 k=20), centres+radius bit-identical across k∈{5,20,50}.** Two stacked levers, both exploiting the matrix being symmetric and column-major: **(a) kernel cache reorder** — `KCentreCDSh_cpp` (`src/kcentre_cdsh.cpp`) score-init (`score[i]=#{j:d(i,j)≤r}`) and the domination-decrement inner loop read *rows* of a column-major matrix (stride-n, a cache miss per access); since `d(i,j)=d(j,i)`, read them through the column pointer `P+i*n` (stride-1), bit-identical — the same T-005 reorder, now interleaved with the per-radius construction. **(b) candidate enumeration ported to C++** — `.KCentreCandidates` was `sort(unique(d[upper.tri(d)]))` (≈28% of the call: an n×n logical mask + R `unique`/`order` over ~n²/2 values); new `KCentreCandidates_cpp` extracts the upper triangle column-sequentially then `std::sort`+`std::unique` → the same sorted distinct set, bit-identical. | profvis (n=2004 k=20, 4 reps): `KCentreCDSh_cpp` 233 samples (~71%), `.KCentreCandidates` 91 (~28%: unique 40 + sort 29 + order 24 + upper.tri 17). `kcentre-verify.R` OLD (optlib) vs NEW (optlib2): 1275→307 ms (**4.16×**); `kcentre-compare.R` idx+radius identical k∈{5,20,50}; `test_local(filter=kcentre)` 70/70 green on the optimised source (brute-force + exact oracles). |
| T-011 | P2 | OPEN | ExactKCentre | `[Optimise]` Not yet profiled. By analogy to T-008 (the dual node-packing solver) the cost is expected to be dominated by the `highs` covering-IP solves, with the R-side `which(d <= r, arr.ind=TRUE)` per-probe coverage enumeration (an n×n logical, mirroring T-008's 933 MB hotspot) the actionable R lever. Next rotation target for area 5. | — (pending; see T-008 for the analogous packing-IP profile). |
| T-009 | — | NOTE | ExactMaxMin | `[AT-LIMIT]` Exact is impractical beyond n≈1000, and **warm-starting cannot fix it** — the wall is the IP, not the probe count. Probe on spam (n=4601, `exact-huge-probe.R`): Grasp+DropAdd both attain the LB in 3.4 s and it sits at the **99.95th percentile** of the distance range (the MaxMin optimum is near the diameter), so the threshold graph at λ* is **near-complete** (~10.6M edges) and the search collapses to a SINGLE infeasibility probe — the best case. That one IP (4601 vars, ~10.6M edge constraints) **did not close in 200 s** (highs "Time limit reached", no incumbent). Edge-formulation node-packing explodes to O(n²) constraints exactly where the optimum lives (high λ ⇒ dense graph); satellite (6435) is worse, letter (20000) is ~200M constraints, shuttle (58000) cannot even build the 27 GB distance matrix. The mid cluster (n≤990) is the practical ceiling. **Only conceivable path:** a clique-cover reformulation — a near-complete graph's millions of edge constraints collapse into a handful of `sum_{i∈C} x_i ≤ 1` clique inequalities — but that is a speculative formulation change, unmeasured, not pursued. | `exact-huge-probe.R` spam n=4601: dist 2.5 s/0.17 GB; 8.85M distinct distances; warm-start LB=47.5269 at i0=8841240/8846079 (99.95%); single probe cand[i0+1] nEdge≈10.6M → highs Time-limit at 207 s, INCONCLUSIVE. |

| T-022 | P1 | DONE | MaxMean | `[Optimise]` **IMPLEMENTED — verified 1.47× end-to-end throughput (786k→1.16M iters/s, n=500, 3 s budget), all tests green, coverage 100%.** MaxMean is time-budgeted, so throughput sets solution quality (more tabu iterations → better `f`; observed f 54.524→54.541). VTune (`result_maxmean`, n=500): per-iteration self-time split ~69% best-flip scan (lines 229–247) / ~31% O(n) P-array update (256–271). Two stacked levers, both verified by isolated `std::chrono` micro-benchmarks at the package's `-O2 -mfpmath=sse` flags: **(a) monotonicity scan** — δ is monotone in `p[i]` (add: ↑ via /(m+1); remove: ↓ via /(m−1)), so the best add is the admissible non-member of largest `p` and the best remove the admissible member of smallest `p`; track those two extrema (aspiration becomes a precomputed `p`-threshold, `gap=best−f≥0`) and divide only the 2 survivors instead of all `n` → 1.13× on the scan, `best_flip`/`best_delta` identical (cross-type exact-δ ties are measure-zero on continuous distances). **(b) branchless P-update** — zero the diagonal once at the R boundary (`diag(dmat)<-0`, a no-op for the objective, which never includes a self-distance) so the `j!=u`/`j!=i` guards in the construction build and both add/remove updates drop → branch-free `p[j]±=col_u[j]` → ≥1.4× on the P-update, **bit-identical** p-arrays (the guard only ever skipped the now-zero diagonal). **REJECTED en route:** plain strength-reduction (precompute `1/(m±1)`, multiply) was **0.97× — slightly slower** (micro-bench `bench-scan.cpp`): the scan is ILP/load-bound at n=500 (all arrays L1-resident), the divide overlaps other work, so VTune's "hot division line" was loads feeding the divide, not the divider port. The win came from doing *less per element* (monotonicity) and *fewer branches* (P-update), not from cheaper arithmetic. | `bench-scan.cpp`: strength-reduction v0=448.8 v1=463.1 ns (**0.97×**, refuted). `bench-scan2.cpp`: monotonicity scan v0=458.3→v2=405.8 ns (**1.13×**, best_flip+delta identical); branchless P-update 1.4×+ (diluted by a common clear loop). End-to-end driver (`drivers/maxmean.R`, optimized -O2 install): 786k→1.16M iters/s (**1.47×**), f 54.524→54.541, `test_dir` all green, covr 157/157+42/42 = 100%. |
| T-012 | P1 | DONE | MaxEntropy (maxdet) | `[Optimise]` **IMPLEMENTED — verified 36% prep gain (3.17→2.02 s, n=1200), selections unchanged.** `MaxEntropy()`'s wrapper cost at large n is two O(n³) eigendecompositions plus an O(n³) reconstruction. Two stacked levers in the new `.MaxEntropyPrepare()` (`R/maxentropy.R`): **(a) merge the redundant eigen** — `.MaxEntropyNegMass` did a values-only `eigen()` (0.49 s) and `.MaxEntropyRepair` a full `eigen()` (1.77 s) on the *same* symmetrised kernel; compute it ONCE and derive negMass from the values (free). **(b) `tcrossprod` reconstruction** — the clip repair `V (Λ Vᵀ)` (general dgemm, 0.91 s) is a symmetric rank-k update `(V√Λ)(V√Λ)ᵀ`; `tcrossprod(V * √λ)` (dsyrk, 0.25 s) is 3.6× faster and identical to 1.78e-15. Both numerically transparent. | Step-timing n=1200 (synthetic non-Euclidean D; eigen FLOPs are size- not value-dependent): negMass 0.49 + repair[eigen 1.77 + recon 0.91] = 3.17 s → merged[eigen 1.77 + tcrossprod 0.25] = 2.02 s (**36.3%**). Verified: `test_local` 180/180 green; greedy pick-for-pick parity with dev prototype `wide-sample/dev/tree_entropy_kernel.R` 12/12 cases; exact 10/11 identical + d3 equal-logdet tie (pre-existing ME-003 scorer tie-break, `tcrossprod`-independent: kernels differ 3.3e-16). |
| T-013 | — | NOTE | MaxEntropy (maxdet) | `[AT-LIMIT]` After T-012 the remaining wrapper cost is **88% the LAPACK symmetric eigendecomposition** (`eigen()`, 1.77 s of 2.02 s at n=1200) — required by the nearest-PSD clip repair (CID is not of negative type, so the full spectrum is needed). Eliminating *all* non-eigen cost would be <12% (under the 20% threshold), and the only sub-eigen levers (randomised/truncated eigendecomposition, a cheaper `shift` repair) change the selection and break dev-prototype parity. Eigen is the floor; rotation skips this area unless the repair model changes. | eigen share 88%; greedy O(n²k) and logdet O(k³) negligible at large n; small-n (n≤120, the realistic MPT use) is sub-10 ms end-to-end — not a profiling target. Driver: `dev/profiling/drivers/maxentropy.R`. |

| T-014 | P1 | DONE | Grasp | `[Optimise]` **IMPLEMENTED — verified 2.03–3.79× at k=100 (n=2000: plateau 8 1.78→0.47 s, 64 5.11→2.36 s, 256 22.59→11.13 s), 1.50–1.90× at smaller shapes, no regression anywhere. 1458/1458 cells bit-identical (indices, objective, `iters`, `pr_calls`), R↔C++ parity 324/324.** Third-order pass beyond T-006/T-007, which left two O(m) inner terms that instrumented counting showed to be 93% (min-to-set) and 7% (tie-break count) of inner ops — and whose *shares swap with `plateau`*, so both ends of the manuscript's plateau ladder had to be covered. **(a) PR walk — hoist the add-to-`pk` minima.** `grasp_path_relink` recomputed `min_to_set(dj_, pk \ {di_})` for every (drop, add) pair, though all those minima come from one add-to-`pk` row; new `near_two`/`near_excl` take the two smallest per add candidate ONCE per walk step, so each exclusion is O(1) and the inner cost loses a whole factor of `|drop_cands|` (Σ over steps O(r³m) → O(r²m)). Dominant at low `plateau`, where phase C is ~90% of wall time and PR's min-to-set was 72% of inner ops. **(b) LS tie-break — split and memoise the pair count.** `count_pairs_le(rem, s, thr)` was O(m²) per surviving candidate; split into `count_pairs_within` (rem×rem, independent of `s`) + `count_to_set_le` (O(m)), memoising the rem×rem half per (ci, thr). `thr` is `best_dstar` on every tie and changes only on the rare improvement, so it recomputes a handful of times per critical position instead of once per candidate — measured 72% memo hit rate. Dominant at high `plateau` (incl. canonical 512), where LS is ~93% of wall time and this term had grown to 43% of min-to-set ops. **Bit-identical**: (a) re-selects the very same matrix cell with no arithmetic (`<` on the min1 update makes `arg1` the vertex a forward scan settles on; a value tying with min1 leaves min2 == min1, so a duplicate minimum correctly survives the exclusion; selections hold distinct vertices, so excluding by vertex id is well defined); (b) is an integer sum of the same comparisons, order-independent. Candidate-evaluation order is untouched, so tie-breaks — and `iters` — are unchanged. | Instrumented op counts (n=2000 dim=10 k=100 eliteSize=10): `crit`/pass = **2.00** exactly (critical positions are the 2 witness endpoints); `count_pairs_le` fire-rate only 0.4–1.4% of scanned candidates (T-006's deferral works) but O(m²/2) per call, so its weight still climbs 6.9% → 30.9% → 42.9% of min-to-set ops at plateau 8 → 64 → 256, while the LS/PR min-to-set split moves 28/72% → 76/24% → 95/5%. Clean-build vs clean-build (base = branch tip, best-of-3): k=100 plateau 8 **3.79×**, 64 **2.17×**, 256 **2.03×**; k=10 plateau 64 0.09→0.06 s **1.50×**; n=500 k=50 0.19→0.10 s **1.90×**. Battery of 1458 cells over n/dim/k/eliteSize/alpha/plateau/seed: 0 mismatches. Full suite 4893 pass / 0 fail. All 7 new C++ branches exercised (both `near_excl` arms, both memo arms). Local *relative* ratios only — canonical wall-clock belongs on Hamilton. |
| T-015 | — | NOTE | Grasp | `[AT-LIMIT]` The same two-smallest hoist **does NOT pay in the local search** — measured and reverted, twice. The LS candidate scan excludes only the critical vertices, and `crit` is **exactly 2**, so the hoist replaces 2 scans of length m−1 with one of length m: a ~2× op-count saving at best, against a `near_two` update measurably dearer per element than the branchless `if (v < best)` min it displaces (the min1/min2/arg1 update cannot fold to a single `minsd`, and its branch is least predictable at small m). Net: **+1.17× at k=100 but 0.64× at k=10** (0.09→0.14 s) — a real regression on a canonical cell for a marginal gain, so T-014(b) ships without it. A second attempt that pulled the strided row into an L1-resident buffer to make both minima branchless was **worse still** (plateau 256: 16.05 s vs 11.13 s shipped): the reads it was meant to save are already cheap, because consecutive `s` share cache lines down each column, so the extra passes over the buffer cost more than the strided access did. Making it uniformly positive needs a per-`s` buffer of `crit` values — unbounded memory in the degenerate high-`crit` case — and restructures the tie-break-order-critical inner loop for ~1.17×. **Not pursued.** Also NOT pursued: incremental maintenance of the minima across LS passes — a swap can evict the argmin, and repairing that exactly needs the 3rd smallest and beyond, so it degrades to a rescan while risking the bit-identity contract. After T-014 the per-pass (n−m)·m full-candidate scan is the inherent floor. | k=10 plateau 64: base 0.090 s, T-014(a+b) 0.070 s, plus LS hoist 0.140 s — isolated by building the LS hoist out separately. Branchless row-buffer variant: k=100 plateau 8/64/256 = 0.65/3.36/16.05 s vs shipped 0.47/2.36/11.13 s. Both rejected variants were themselves 1458/1458 bit-identical, so the choice was purely cost, not correctness. |

Grasp (area 3): see log.md for its rounds.
