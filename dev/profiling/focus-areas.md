# Profiling focus areas — MaxMin

Ranked hot paths for the `/profile` rotation. The package sells on speed across
three solvers; the rotation must touch all three on realistically-sized inputs,
each at a **small** and a **large** `k` (= `n`/`m`, the subset size), and on both
the dense-matrix and matrix-free (`points=`) paths where they exist.

| # | Area | Files | Why hot (1 line) | Paths | k regimes | Last profiled | Status |
|---|------|-------|------------------|-------|-----------|---------------|--------|
| 1 | FarFirst greedy + ensemble | `src/maximin.cpp`, `src/maximin_points.cpp`, `src/utils.cpp`, `R/seed.R`, `R/farfirst.R` | O(N·n·dim) greedy pass run 3× per default ensemble; O(N²·dim) seeding anchors | matrix, points, column-oracle | n/N ∈ {0.01 … 0.5} | 2026-08-13 | AT-LIMIT serial, certified on both axes (rounds 1+7 instruction axis: free-T_k, squared space, argmax fold + fused sweep; round 9 validation/memory axis: allocation-free finite scan — matrix cell 5.1×, blocked dimension sweeps 1.3–1.4×, triangle/squared-guard + fused anchor sweeps — anchors 2.6×/4.7×; the alternative orientations measured equal or slower: matrix-pass threads slower at every feasible N, register-banded row aggregates identical, Gram/gemv excluded-by-contract). mc.cores kernels nCores-invariant (round 7; round 9: N=1e5 pass d10 85 ms, anchors 60 ms at 8T). Remaining axis: Hamilton. See log.md rounds 1, 7, 9 |
| 2 | DropAdd tabu search | `src/dropadd.cpp`, `src/dropadd_mf.cpp`, `R/dropadd.R` | `plateau`(5000) iterations, each O(n) record updates; points path O(n·dim) per column | matrix, points, column-oracle | m ∈ {small, ~n/2} | 2026-08-13 | OPTIMISED points path serial + mc.cores (round 10: blocked fills + sqrt-at-consumption 1.08–1.27×; fused-pass threading 2.3× construction / up to 1.75× end-to-end at 8T, nCores-invariant, n ≥ 16384). Matrix kernel AT-LIMIT (seed at DRAM bandwidth — T-005a; fused argmax and threading both measured flat: record arrays cache-resident, column streams one-core-saturated; lazy-min2 recompute lead recorded). Oracle harness ≤7% of a realistic colFn wall — at-limit by design. See log.md rounds 2, 4, 10 |
| 3 | Grasp refinement | `src/grasp.cpp`, `R/grasp.R` | extended-improvement local search scans (n−m) candidates × O(m²) sub-matrix; PR over elite pairs | matrix only | m ∈ {small, large} | 2026-08-13 | AT-LIMIT serial, certified on both axes (rounds 3–6 instruction axis: VTune, every hot loop a semantics-mandated single pass; round 8 memory axis: the symmetric-transpose alternative for the rescan reads measured 0.25–0.75× — slower everywhere — and was reverted, so the shipped selected-columns access pattern is the cache-optimal orientation). Remaining axis: cores + Hamilton. See log.md rounds 5–6, 8 |
| 4 | ExactMaxMin node-packing | `R/exact.R`, `src/exact_reduce.cpp` | threshold search in R; every probe decided by the C++ clique search | matrix only | m ∈ {2,4,6,10} (exact regime) | 2026-08-14 | OPTIMISED (round 5: sparse-A + Grasp warm-start gallop, 19.6× grid, T-008; round 11: (k−1)-core peel + colouring bound + clique-strengthened per-component IPs, grid ~2×; round 12: IP replaced by an exhaustive colour-bounded clique search, warm-start-tail candidate grid, C++ triangle scans — scores bit-identical 56/56, grid 7.2×, vowel k10 171→0.2 s, satellite n=6435 k=10 newly reachable; no `highs` on the solve path; root-branch parallelism measured at ~1.2× end-to-end and declined. See log.md rounds 5, 11, 12) |
| 5 | KCentre CDSh + ExactKCentre | `src/kcentre_cdsh.cpp`, `src/kcentre_cover.cpp`, `R/kcentre.R` | CDSh: O(n² log n), per-radius O(n²) dominating-set construction with cache-hostile row scans + R-side `sort(unique(upper.tri))`. ExactKCentre: threshold bisection in R, every probe decided by the C++ covering search (dual of area 4). | matrix only | k ∈ {small, n/2} | 2026-08-19 | CDSh OPTIMISED (cache reorder + C++ candidates, 4.16×; T-010). ExactKCentre OPTIMISED (round 16: the per-probe set-cover IP replaced by `CoverDecide_cpp` — unit propagation, point/centre dominance to a fixpoint, component split, bounded exhaustive search; grid 11.3× on Hamilton, 56/56 radii bit-identical, 707/707 probe verdicts matching the IP; no `highs` or `Matrix` on the solve path; T-011 closed). **Residue is now the warm start, not the search**: `KCentre()`'s exhaustive-scan path (n ≤ 150) dominates those cells while the whole threshold search is milliseconds — a measured, unshipped lead. The search's own wall is refutation at larger k (n=200 in 5-d: k=10 → 7.4M nodes, k=14 → 1.2×10⁸): a colour-style bound imported from area 4 is the untried lever. See log.md round 16 |
| 6 | MaxMean RLTS tabu loop | `src/maxmean.cpp`, `R/maxmean.R` | Time-budgeted: throughput (iters/s) sets solution quality. Per tabu iteration O(n): best-flip scan (per-element division by `m±1`, 3-array gather p/in_S/tabu_until) + O(n) P-array update over column u. | matrix only | size free (RLTS) | 2026-08-14 | OPTIMISED (monotonicity scan + branchless P-update, 1.47× throughput; T-012). `maxIter` `iter_cap` guard added 2026-08 and verified throughput-neutral by on-machine A/B (log 2026-08-14) — no issue |

Status legend: `NEW`, `PROFILED`, `OPTIMISED`, `AT-LIMIT`, `SKIPPED`.

Notes
- Realistic sizes: FarFirst/DropAdd scale to large n (points path is O(n) memory);
  Grasp is dense-only and intended for small n (materialises n×n repeatedly).
- "small k" exercises the construction/per-iteration O(n) cost; "large k"
  exercises the O(m²)/recompute terms that dominate at high subset fraction.
- Drivers must stay ≤ ~8 s bare (bound DropAdd/Grasp with `plateau`/`maxIter`).
