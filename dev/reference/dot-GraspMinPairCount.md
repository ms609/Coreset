# Count pairs at the minimum distance (used by extended-improvement LS).

Count pairs at the minimum distance (used by extended-improvement LS).

## Usage

``` r
.GraspMinPairCount(d, sel, dstar)
```
