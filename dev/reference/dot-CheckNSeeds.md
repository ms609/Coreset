# Validate a number of random-furthest seeds

Validate a number of random-furthest seeds

## Usage

``` r
.CheckNSeeds(nSeeds)
```

## Arguments

- nSeeds:

  The `nSeeds` argument as supplied.

## Value

`.CheckNSeeds()` returns `nSeeds` as a single positive integer, or
stops.
