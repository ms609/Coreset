# Validate the element count of a distance-column function

Validate the element count of a distance-column function

## Usage

``` r
.CheckColumnN(N)
```

## Arguments

- N:

  The `N` argument as supplied.

## Value

`.CheckColumnN()` returns `N` as a single positive integer, or stops.
